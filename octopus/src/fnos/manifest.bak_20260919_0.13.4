appname                    = octopus
version                    = 0.13.4-1
display_name               = Octopus
desc                       = One Hub All LLMs For You —— LLM API 聚合网关，Go 单二进制，SQLite 存储，原生宿主机运行。上游 v0.13.4（请求头新增 client_header + 渠道禁用 + 前端日志 UI 优化）+ 本地 gateway sidecar v8 剥前缀 HTTP 反代（SSE 流式透传 + 单模块图单实例），手机端 /app/octopus 全链路可路由。
platform                   = x86
source                     = thirdparty
maintainer                 = bestruirui
maintainer_url             = https://github.com/bestruirui/octopus
distributor                = Mike
distributor_url            = https://github.com/Mike-hd123
os_min_version             = 0.9.27
ctl_stop                   = true
micro_app                  = true
checkport                  = false
service_port               = 8081
desktop_uidir              = ui
desktop_applaunchname      = octopus.panel
disable_authorization_path = false
changelog                  = v1（2026-09-10）：同步上游 v0.13.4（请求头新增 client_header、渠道禁用 disable channel、前端日志 UI 优化），保留本地 gateway sidecar v8 补丁（剥前缀 HTTP 反代 + SSE 透传 + JS chunk 同版本参数单模块图单实例，修复手机端 404 与桌面白屏）。上游大版本升级，本地补丁版本号重置为 1。
